// Abrir el modal de perfil del actor
function verPerfil(actorId) {
    document.getElementById('actorProfileModal').style.display = 'block';
    // Aquí puedes cargar dinámicamente la información del actor con `actorId`
}

// Cerrar el modal
function cerrarModal() {
    document.getElementById('actorProfileModal').style.display = 'none';
}
