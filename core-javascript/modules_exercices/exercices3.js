user = " Diametro de una rueda y grosor"

let diameter 
let diameter 

if (diameter > 1.4)
    console.log("La rueda es para un vehiculo grande")
else if (diametro <= 1.4) {
    console.log("La rueda es para un vehiculo mediano")
} else {
    return "La rueda es para un vehiculo pequeño"
}

else if (diameter > 1.4) || (grosor < 0.4) && (diameter <= 1.4) || (diameter > 0.8) || (grosor < 0.25) {
    console.log("El grosor para esta rueda es inferior al recomendado")

}


username = window.prompt(" whatb is your user name?")